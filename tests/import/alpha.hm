
module gamma = 'gamma';

import { name: gammaName } from gamma,
       { name: betaName, func: betaFunc, uniqueBeta } from 'beta';

export gammaName;

export betaName;

export betaFunc;

export uniqueBeta;
